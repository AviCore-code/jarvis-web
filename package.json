{
  "name": "jarvis-web",
  "version": "1.0.2",
  "description": "Jarvis face-to-face voice+video web app, backed by Hermes Agent API Server",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test ops/reverse-proxy/proxy.test.js && python3 -m unittest discover -s ops/location-tracker -p 'test_*.py' && python3 ops/tailscale/test_watchdog.py"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^2.7.0",
    "dotenv": "^16.4.5",
    "multer": "^2.0.0",
    "cors": "^2.8.5",
    "form-data": "^4.0.1",
    "http-proxy": "^1.18.1"
  }
}
